namespace JsStore {
    export namespace Business {
        export class Transaction extends Base {
            create(tableNames: string[], txLogic) {
                // is_transaction_active = true;
                // this._transaction.
            }
        }
    }
}
